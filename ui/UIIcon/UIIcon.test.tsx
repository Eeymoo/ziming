import { faUser } from '@fortawesome/free-solid-svg-icons'
import type { UIIconProps, UIIconProviderProps } from './types'

const iconProps: UIIconProps = {
  icon: faUser,
  size: 24,
}

const providerProps: UIIconProviderProps = {
  color: '#007F3D',
}

void iconProps
void providerProps
